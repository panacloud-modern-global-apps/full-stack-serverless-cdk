export declare const pixelThreshold = 10;
export declare const selectors: {
    formFieldDescription: string;
};
export declare const knobs: {
    labelKnob(labelPlaceholder: string): string;
    descriptionKnob(descriptionPlaceholder: string): string;
    hintKnob(hintPlaceholder: string): string;
    inputTypeKnob(inputTypePlaceholder: string): string;
    placeholderKnob(placeholderPlaceholder: string): string;
    buttonTypeKnob(buttonTypePlaceholder: string): string;
    buttonTextKnob(buttonTextPlaceholder: string): string;
    toggleKnob(toggleDefault: boolean): boolean;
    disabledKnob(disabledDefault: boolean): boolean;
    formSectionHeaderKnob(headerPlaceholder: string): string;
    formSectionSubmitKnob(submitPlaceholder: string): string;
    tooltipAutoShowKnob(autoShowDefault: boolean): boolean;
};
